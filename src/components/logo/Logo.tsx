import styles from './Logo.module.css';

const Logo = () => {
    return (
        <div className={styles.logo}>
            <h2>
                <span>Martina</span>
                <span>Facco</span>
            </h2>
        </div>
    );
};

export default Logo;
